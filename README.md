# interface_tcc
Interface web (HTML/CSS) para telemetria rural via ESP32. Mostra visualmente nível, vazão e controle da bomba.  // Web interface (HTML/CSS) for ESP32 rural telemetry. Visually displays water level, flow, and pump control.
